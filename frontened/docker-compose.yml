
services:
  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: npx json-server --watch db.json --host 0.0.0.0 --port 4000
    ports:
      - "4000:4000"